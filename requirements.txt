# Stage 0-1 (discover + triage) need only requests.
requests>=2.31

# Stage 2-4 (fetch, parse, store).
# NB: latest pymatgen on PyPI is 2026.5.4 (ships pymatgen-core 2026.5.18); the
# "v2026.5.18" in CLAUDE.md is a source-tree tag, not a release version.
pymatgen>=2026.5.4    # Vasprun/Outcar parsing, converged_electronic, run_type
ase>=3.25             # extxyz(.gz) read/write, OUTCAR trajectory fallback
tqdm>=4.66            # progress bars for long harvests

# Optional: consolidate metadata.jsonl -> parquet for fast column queries.
# pyarrow>=15
